# Hypothesis-thesting-brain-size
This Python code performs hypothesis tests using the `scipy.stats` module to analyze the brain size dataset provided in the `brain_size.csv` file (Dietz et al., 2017).
